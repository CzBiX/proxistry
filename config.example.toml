[server]
# Address and port to listen on
# listen = "0.0.0.0:5000"
# External base URL of the proxy, useful when running behind a reverse proxy with a different hostname or HTTPS.
# base_url = "http://{listen}"
# Maximum number of concurrent requests per upstream registry
# concurrent = 10

[cache]
# Directory for cached data (manifests, blobs, index)
# data_dir = "./cache"
# Maximum cache size in GB; LRU eviction runs when exceeded
# max_size_gb = 4
# Tag-to-digest index TTL (how long a tag mapping is considered fresh)
# Supports human-readable durations: "10m", "1h", "30s", etc.
# Set to "0s" to disable expiration (never expire)
# tag_ttl = "30m"
# Digest manifest TTL (manifests fetched by digest are immutable)
# manifest_ttl = "7d"
# Blob TTL (blobs are content-addressed and immutable)
# blob_ttl = "7d"

[whitelist]
# When enabled, only registries listed here can be proxied
# enabled = false
# registries = ["docker.io", "ghcr.io"]

# --- Registry Configurations ---
# Each [[registries]] block defines an upstream registry.

#[[registries]]
#name = "docker.io"
#[registries.auth]
#type = "basic"
#username = "user"
#password = "pass"

#[[registries]]
#name = "ghcr.io"
#[registries.auth]
#type = "basic"
#username = "user"
#password_file = "/secret/ghcr_pass.txt"